# 项目目标

- 开发一个以现有 Excel 宝可梦数据库为数据源的宝可梦数据查询和计算工具。
- 优先保证轻量、启动和加载迅速、离线运行、低资源占用、长期可维护和良好可扩展性。
- 第一阶段以本地静态 Web App 为目标。
- 暂定技术栈为 Svelte、TypeScript、Vite 和静态 JSON 数据。
- 第一阶段不使用后端服务器、云数据库、SQLite、Electron、Tauri、React、Vue、大型 UI 框架或不必要的状态管理库。

# 用户背景与沟通

- 用户完全没有软件开发经验。
- 完成每个任务后，使用中文说明：做了什么、为什么这样做、修改了哪些主要文件、如何手动验证、自动检查或测试结果，以及是否存在已知问题。
- 不要因为用户没有开发经验而降低代码质量。
- 技术解释应优先结合当前项目，无需扩展成完整教程。
- 可以通过读取项目文件、运行检查或测试自行确认的问题，应优先自行确认，不要把技术判断全部交给用户。

# 开发原则

- 一个任务尽量只解决一个明确问题，不要无理由修改与当前任务无关的文件。
- 优先使用最简单、清晰、容易维护且满足需求的实现，不要为了展示高级架构而增加复杂度。
- 避免过度工程化，不要擅自引入大型依赖。
- 新增依赖前必须确认确有必要，并在完成报告中说明其用途。
- 数据层、业务逻辑层和 UI 层应尽量分离。
- 不要把大量业务计算逻辑直接写进 Svelte UI 组件；核心计算逻辑应优先实现为独立 TypeScript 模块和纯函数。
- 使用明确的 TypeScript 类型，避免不必要的 `any`。
- 合理抽取重复逻辑，但不要为了抽象而抽象。

# 数据安全

- `data-source/Pokemon-data.xlsx` 是项目的数据源副本，默认视为只读输入。
- 永远不要覆盖、修改、删除或重写该 Excel 文件，除非用户以后明确要求。
- 数据转换产生的文件必须写入其他目录。
- 不要为了让程序运行而偷偷修正 Excel 原始数据。
- 发现数据异常时，应报告问题和来源；不要静默忽略，也不要凭宝可梦常识猜测并覆盖原始数据。
- 迁移时必须区分 Excel 原始数据、公式计算结果和展示逻辑。
- 可以实时计算得到的衍生数据，原则上不要在新数据结构中重复保存。

# 数据模型原则

- 必须明确区分 Pokemon Species 和 Pokemon Form。
- 数据模型应为普通形态、地区形态、Mega、超极巨化、性别差异、特殊形态、多分支进化和特殊进化条件保留扩展能力。
- 不要仅为迁移方便而把 Excel 每一列机械复制到 JSON。
- JSON 应是规范化后的程序数据，而不是 Excel 的直接镜像。
- 实体引用应尽可能使用稳定 ID，不要依赖显示名称作为唯一关联方式。

# 性能与轻量化

- 项目数据规模较小，优先采用简单方案。
- 第一阶段尽量保持纯前端和静态数据，不要因性能假设提前引入数据库或后端。
- 避免大型依赖、不必要的运行时、远程字体和必须联网的 UI 资源。
- 后续正式 UI 必须确保离线时仍可完整使用。

# 测试与验证

- 修改代码后必须运行与当前任务相关的检查。
- 建立 Svelte/TypeScript 项目后，应至少根据项目实际情况运行 TypeScript/Svelte 检查、自动测试和 production build。
- 核心计算逻辑必须有自动测试，数据转换必须有数据验证。
- 数据转换中禁止静默丢弃无法识别的数据。
- 对边界情况和复杂宝可梦形态进行人工抽查。
- 测试失败时先查找根因，不要通过删除测试、降低断言标准或绕开逻辑让测试通过。

# 调试原则

- 出现问题时先复现，并检查终端、编译、测试及必要的浏览器错误信息。
- 不要随机修改代码；优先实施最小修复。
- 修复后添加适当的回归测试。
- 不要通过删除功能或降低验证标准掩盖问题。

# Git 原则

- `data-source/*.xlsx` 保持不进入 Git。
- `node_modules/`、构建产物、缓存和私密环境变量文件不得进入 Git。
- 一个 commit 尽量对应一个明确、稳定的阶段或功能。
- 创建 commit 前必须运行必要测试或检查、检查 `git diff`，并确认没有意外修改。
- 不要擅自执行破坏性 Git 操作，例如 `git reset --hard`、`git clean -fd`、强制推送或重写历史，除非用户明确要求并理解影响。
- 不要在未确认的情况下删除用户现有文件。

# 当前阶段限制

当前项目仍处于初始化阶段。在用户明确进入下一阶段前：

- 不要创建 Svelte/Vite 项目。
- 不要安装项目依赖。
- 不要分析 Excel。
- 不要创建数据转换程序。
- 不要开始 UI 开发。
